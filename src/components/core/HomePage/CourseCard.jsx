const CourseCard = ({ cardData }) => {
  return (
    <div className="">
      {/* {cardData.heading}
      {cardData.description}
      {cardData.level}
      {cardData.lessionNumber} */}
    </div>
  );
};

export default CourseCard;
